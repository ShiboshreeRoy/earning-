<div class="container h-screen flex items-center justify-center bg-gray-100">
  <div class="bg-white shadow-md rounded-lg p-8 w-full max-w-sm">
<h2 class="text-3xl font-bold mb-6 text-gray-800 text-center">Log in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name), class: "space-y-6 max-w-md mx-auto") do |f| %>
  <!-- Email Field with Icon -->
  <div class="field">
    <label for="email" class="block text-sm font-semibold text-gray-700 flex items-center">
      <i class="fas fa-envelope text-gray-500 mr-3"></i>
      Email
    </label>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" %>
  </div>

  <!-- Password Field with Icon -->
  <div class="field">
    <label for="password" class="block text-sm font-semibold text-gray-700 flex items-center">
      <i class="fas fa-lock text-gray-500 mr-3"></i>
      Password
    </label>
    <%= f.password_field :password, autocomplete: "current-password", class: "w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" %>
  </div>

  <!-- Remember Me Option -->
  <% if devise_mapping.rememberable? %>
    <div class="field flex items-center space-x-2">
      <%= f.check_box :remember_me, class: "h-4 w-4 text-blue-500 border-gray-300 rounded" %>
      <%= f.label :remember_me, class: "text-sm text-gray-700" %>
    </div>
  <% end %>

  <!-- Submit Button -->
  <div class="actions">
    <%= f.submit "Log in", class: "w-full py-2 px-4 bg-blue-600 text-white rounded-md hover:bg-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
  </div>
<% end %>

<!-- Links (forgot password, etc.) -->
<%= render "devise/shared/links", class: "mt-4 text-sm text-center text-blue-600 hover:underline" %>

<!-- Optional Social Media Buttons (Optional for Real-World Use) -->
<div class="flex justify-center space-x-4 mt-6">
  <%= link_to '#', class: "flex items-center gap-2 bg-gray-800 text-white rounded-md px-4 py-2" do %>
    <i class="fab fa-facebook-f"></i> Facebook
  <% end %>
  <%= link_to '#', class: "flex items-center gap-2 bg-blue-600 text-white rounded-md px-4 py-2" do %>
    <i class="fab fa-twitter"></i> Twitter
  <% end %>
</div>

</div>